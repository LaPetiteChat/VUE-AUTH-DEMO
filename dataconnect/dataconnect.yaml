specVersion: "v1beta"
serviceId: "vue-auth-demo"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "vue-auth-demo-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
